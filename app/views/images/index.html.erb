<h1>Listing Images</h1>

<table>
  <% @images.each do |image| %>
    <tr>
      <td><%= link_to (image_tag image.url, class: 'img-panel'), image %><td>
      <td><%= link_to image.tag_list, class: 'tag_name'  %></td>
    </tr>
  <% end %>
</table>

