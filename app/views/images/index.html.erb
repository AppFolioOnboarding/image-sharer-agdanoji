<h1>Listing Image links</h1>

<table>
  <tr>
    <th>Links</th>
    <th></th>
  </tr>

  <% @images.each do |image| %>
    <tr>
      <td><%= image.url %></td>
      <td><%= link_to 'Show', image_path(image) %></td>
    </tr>
  <% end %>
</table>

