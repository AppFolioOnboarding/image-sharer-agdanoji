<h1>Listing Images</h1>

<table>
  <tr>
    <th></th>
  </tr>

  <% @images.each do |image| %>
    <tr>
      <td><%= link_to (image_tag image.url, class: 'img-panel'), image %><td>
    </tr>
  <% end %>
</table>

